import { useState } from 'react'
import { articles, faqs, gsWatchlist, siteConfig, watchlistCategories } from './data'
import './App.css'

type Page = 'home' | 'about' | 'watchlist' | 'blog' | 'privacy'
const navItems: { id: Page; label: string }[] = [
  { id: 'home', label: 'Home' }, { id: 'about', label: 'About FUP' }, { id: 'watchlist', label: 'Watchlist' }, { id: 'blog', label: 'Journal' },
]

function App() {
  const [page, setPage] = useState<Page>('home')
  const [menuOpen, setMenuOpen] = useState(false)
  const navigate = (nextPage: Page) => { setPage(nextPage); setMenuOpen(false); window.scrollTo({ top: 0, behavior: 'smooth' }) }
  return <div className="app-shell">
    <div className="announcement">A free, focused space for serious aspirants <span>•</span> Built for the long road</div>
    <header className="site-header">
      <button className="wordmark" onClick={() => navigate('home')} aria-label="Go to home"><span>F</span>UP<small>free upsc preparation</small></button>
      <button className="menu-toggle" onClick={() => setMenuOpen(!menuOpen)} aria-label="Toggle navigation">{menuOpen ? '×' : '☰'}</button>
      <nav className={menuOpen ? 'open' : ''}>{navItems.map(item => <button className={page === item.id ? 'active' : ''} key={item.id} onClick={() => navigate(item.id)}>{item.label}</button>)}<a className="nav-join" href={siteConfig.telegramUrl} target="_blank" rel="noreferrer">Join community <span>↗</span></a></nav>
    </header>
    <main>{page === 'home' && <Home navigate={navigate} />}{page === 'about' && <About navigate={navigate} />}{page === 'watchlist' && <Watchlist />}{page === 'blog' && <Blog />}{page === 'privacy' && <Privacy />}</main>
    <footer><div><button className="wordmark footer-mark" onClick={() => navigate('home')}><span>F</span>UP<small>free upsc preparation</small></button><p>Good preparation should be<br />accessible to everyone.</p></div><div className="footer-links"><span>Explore</span><button onClick={() => navigate('about')}>About FUP</button><button onClick={() => navigate('watchlist')}>Watchlist</button><button onClick={() => navigate('blog')}>Journal</button></div><div className="footer-links"><span>Connect</span><a href={siteConfig.telegramUrl} target="_blank" rel="noreferrer">Telegram ↗</a><a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a><button onClick={() => navigate('privacy')}>Privacy policy</button></div><div className="footer-bottom"><span>© 2026 FUP. Made for the journey.</span><span>freeupsc.online</span></div></footer>
  </div>
}
function Button({ children, onClick, ghost = false }: { children: React.ReactNode; onClick?: () => void; ghost?: boolean }) { return <button className={`button ${ghost ? 'button-ghost' : ''}`} onClick={onClick}>{children} <span>↗</span></button> }
function Home({ navigate }: { navigate: (page: Page) => void }) { return <>
  <section className="hero"><div className="hero-copy"><p className="eyebrow">A better way to prepare</p><h1>Prepare with<br /><em>clarity.</em> Progress<br />with purpose.</h1><p className="hero-intro">Free, thoughtfully curated resources for UPSC aspirants who are ready to show up for their future, one day at a time.</p><div className="hero-actions"><Button><a href={siteConfig.telegramUrl} target="_blank" rel="noreferrer">Join the community</a></Button><Button ghost onClick={() => navigate('watchlist')}>Explore watchlist</Button></div></div><div className="hero-art"><div className="sun"></div><div className="art-note">For the<br /><strong>long road.</strong></div><div className="art-stamp">100%<br /><small>free to use</small></div><div className="art-lines"></div></div><div className="hero-scroll">Scroll to explore <span>↓</span></div></section>
  <section className="trust-strip"><span>Designed for the UPSC journey</span><strong>UPSC CSE</strong><strong>GS I — IV</strong><strong>NCERTs</strong><strong>Current Affairs</strong><strong>Answer Writing</strong></section>
  <section className="section intro-section"><div className="section-label">01 / The FUP approach</div><div className="intro-grid"><h2>Less noise.<br /><em>More direction.</em></h2><div><p className="large-copy">The internet has enough information. What aspirants need is a clear path through it.</p><p>FUP brings the essentials together, removes the overwhelm and gives you a steady place to begin, return to and grow from.</p><button className="arrow-link" onClick={() => navigate('about')}>Our approach <span>↗</span></button></div></div></section>
  <section className="resource-section"><div className="section resource-head"><div className="section-label">02 / What we provide</div><h2>Everything you need<br />to <em>start strong.</em></h2></div><div className="resource-grid">{[['01', 'Study material', 'Short, reliable notes and resources that respect your time.'], ['02', 'Newspaper guidance', 'Learn what matters, why it matters and how to remember it.'], ['03', 'NCERT direction', 'A no-confusion path through the foundational texts.'], ['04', 'YouTube watchlist', 'Hand-picked videos, organized by subject and stage.']].map(([number, title, text]) => <div className="resource-card" key={number}><span>{number}</span><h3>{title}</h3><p>{text}</p><b>↗</b></div>)}</div></section>
  <section className="section method-section"><div className="section-label">03 / A simple rhythm</div><div className="method-grid"><div><h2>Preparation is<br /><em>a practice.</em></h2><p className="large-copy">The best strategy is one you can return to on an ordinary Tuesday.</p></div><div className="steps">{[['01', 'Choose your focus', 'Start with one subject, one source and one achievable goal.'], ['02', 'Build your base', 'Let clear concepts come before speed, volume or comparison.'], ['03', 'Keep showing up', 'Small, consistent sessions compound into real confidence.']].map(([n, t, d]) => <div className="step" key={n}><span>{n}</span><div><h3>{t}</h3><p>{d}</p></div></div>)}</div></div></section>
  <section className="quote-section"><div className="quote-mark">“</div><blockquote>We are not here to make preparation look easy. We are here to make the next step feel clear.</blockquote><p>— The FUP promise</p></section>
  <section className="section faq-section"><div className="section-label">04 / Questions, answered</div><div className="faq-grid"><h2>Clarity feels<br /><em>lighter.</em></h2><div>{faqs.map(([question, answer]) => <details key={question}><summary>{question}<span>+</span></summary><p>{answer}</p></details>)}</div></div></section>
  <section className="final-cta"><p className="eyebrow">Your journey starts here</p><h2>One good decision<br /><em>can change the year.</em></h2><Button><a href={siteConfig.telegramUrl} target="_blank" rel="noreferrer">Join FUP on Telegram</a></Button></section>
</> }
function PageHero({ label, title, intro }: { label: string; title: React.ReactNode; intro: string }) { return <section className="page-hero"><p className="eyebrow">{label}</p><h1>{title}</h1><p className="page-intro">{intro}</p></section> }
function About({ navigate }: { navigate: (page: Page) => void }) { return <><PageHero label="About FUP" title={<>A little more<br /><em>intention.</em></>} intro="FUP is a free preparation companion for aspirants who want a calmer, more considered way to prepare for the civil services." /><section className="section about-body"><div className="section-label">Our reason for being</div><div><p className="large-copy">UPSC preparation can be lonely, expensive and unnecessarily confusing.</p><p>We believe the first step towards a better preparation ecosystem is simple: make the right information easier to find. FUP is our attempt to do that with care. No noise. No false promises. Just useful resources and a community that understands the long road.</p><Button onClick={() => navigate('watchlist')}>Explore the watchlist</Button></div></section><section className="values"><div><span>01</span><h2>Useful over<br /><em>impressive.</em></h2></div><div><span>02</span><h2>Progress over<br /><em>perfection.</em></h2></div><div><span>03</span><h2>Community over<br /><em>competition.</em></h2></div></section></> }
function Watchlist() { const [active, setActive] = useState('gs'); const [activeSubject, setActiveSubject] = useState(gsWatchlist[0].subject); const [activeVideo, setActiveVideo] = useState(0); const category = watchlistCategories.find(item => item.id === active)!; const isGs = active === 'gs'; const selectedResource = gsWatchlist.find(resource => resource.subject === activeSubject) ?? gsWatchlist[0]; const videos = selectedResource.videos ?? [{ title: selectedResource.firstVideoTitle, videoId: selectedResource.firstVideoId }]; const selectedVideo = videos[activeVideo] ?? videos[0]; const embedUrl = selectedVideo.videoId ? `https://www.youtube.com/embed/${selectedVideo.videoId}?list=${selectedResource.playlistId}&rel=0` : `https://www.youtube.com/embed?list=${selectedResource.playlistId}&index=${selectedVideo.index}&rel=0`; const selectSubject = (subject: string) => { setActiveSubject(subject); setActiveVideo(0) }; const selectStream = (streamId: string) => { setActive(streamId); if (streamId === 'gs') { setActiveSubject(gsWatchlist[0].subject); setActiveVideo(0) } }; return <><PageHero label="The FUP watchlist" title={<>Watch less.<br /><em>Learn more.</em></>} intro="A hand-picked library of videos that will eventually make your YouTube hours count. Organized by what you are actually trying to learn." /><section className={`section watch-body ${isGs ? 'watch-body-gs' : ''}`}><div className="category-list"><div className="category-heading">Choose a stream</div>{watchlistCategories.map(item => <button className={active === item.id ? 'selected' : ''} onClick={() => selectStream(item.id)} key={item.id}><span>{item.short}</span>{item.label}<b>↗</b></button>)}{isGs && <div className="subject-list"><div className="category-heading">GS subjects</div>{gsWatchlist.map((resource, index) => <button className={`subject-button ${selectedResource.subject === resource.subject ? 'selected' : ''}`} onClick={() => selectSubject(resource.subject)} key={resource.subject}><span>{String(index + 1).padStart(2, '0')}</span><strong>{resource.subject}</strong><small>{resource.source}</small></button>)}</div>}</div>{isGs ? <div className="gs-content"><div className="gs-heading"><div><div className="coming-badge">Curated for GS</div><h2>General Studies<br /><em>with intention.</em></h2></div><p>{category.description} Start with one reliable source for each core subject and build steadily.</p></div><div className="gs-player"><div className="gs-player-meta"><div><span className="section-label">Now studying · {selectedResource.source}</span><h3>{selectedResource.subject}</h3><p>{selectedVideo.title}</p></div><span className="gs-player-note">{videos.length} videos in playlist</span></div><div className="gs-embed"><iframe src={embedUrl} title={`${selectedResource.subject} UPSC playlist`} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen></iframe></div></div><div className="playlist-panel"><div className="playlist-panel-head"><span className="section-label">{selectedResource.subject} playlist</span><a href={selectedResource.playlistUrl} target="_blank" rel="noreferrer">Open on YouTube ↗</a></div><div className="playlist-items">{videos.map((video, index) => <button className={activeVideo === index ? 'selected' : ''} onClick={() => setActiveVideo(index)} key={`${video.title}-${index}`}><span>{String(index + 1).padStart(2, '0')}</span><strong>{video.title}</strong><b>{activeVideo === index ? 'Playing' : 'Watch'} ↗</b></button>)}</div></div></div> : <div className="watch-empty"><div className="coming-badge">Coming soon</div><h2>{category.label}<br /><em>with intention.</em></h2><p>{category.description}</p><div className="empty-rule"></div><small>Hand-picked videos are being reviewed and organized.<br />This space will be worth the wait.</small></div>}</section></> }
function Blog() { const [filter, setFilter] = useState('All'); const categories = ['All', ...new Set(articles.map(article => article.category))]; const filtered = filter === 'All' ? articles : articles.filter(article => article.category === filter); return <><PageHero label="The FUP journal" title={<>Notes for the<br /><em>long road.</em></>} intro="Thoughts, frameworks and useful explainers for making your preparation a little more deliberate." /><section className="section blog-body"><div className="filter-row">{categories.map(category => <button className={filter === category ? 'selected' : ''} onClick={() => setFilter(category)} key={category}>{category}</button>)}</div><div className="article-grid">{filtered.map((article, index) => <article className={index === 0 ? 'featured-article' : ''} key={article.slug}><div className="article-image"><span>{article.category}</span><b>{String(index + 1).padStart(2, '0')}</b></div><div className="article-meta">{article.date}<span>•</span>{article.readTime}</div><h2>{article.title}</h2><p>{article.excerpt}</p><button className="arrow-link">Read note <span>↗</span></button></article>)}</div></section></> }
function Privacy() { return <><PageHero label="Privacy policy" title={<>Your trust is<br /><em>not data.</em></>} intro="A short, clear explanation of how FUP handles the little information you share with us." /><section className="section legal-copy"><p className="large-copy">We keep things simple.</p><h3>What we collect</h3><p>FUP does not require an account to browse the website. If you contact us by email, we only use your message to respond to you. Telegram is governed by Telegram's own privacy policy.</p><h3>Analytics and cookies</h3><p>We aim to keep this website lightweight. If analytics are added in the future, this policy will be updated before they are used.</p><h3>Contact</h3><p>Questions about privacy can be sent to <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a>.</p></section></> }

export default App
